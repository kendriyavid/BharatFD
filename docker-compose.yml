version: "3.8"

services:
  redis:
    image: redis:latest
    container_name: redis-local
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: ["redis-server"]

volumes:
  redis_data:
    driver: local
